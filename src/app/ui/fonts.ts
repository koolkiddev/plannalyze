import { Outfit } from 'next/font/google';
import { Karla } from 'next/font/google'; 

export const outfit = Outfit({ subsets: ['latin'], weight: ['400'] });
export const karla = Karla({ subsets: ['latin'], weight: ['800'] });